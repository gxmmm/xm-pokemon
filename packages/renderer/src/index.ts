/** Renderer contracts. These are intentionally implementation-neutral: a
 * compatibility Canvas renderer and a GPU renderer must receive the same
 * snapshots and ordered cue stream. */
export * from './contracts.ts';
export * from './capabilities.ts';
