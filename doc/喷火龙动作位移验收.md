# 喷火龙动作位移验收（2026-09-11）

本轮重新尝试生成喷火龙的待机、蓄力、释放、恢复四张姿态，并检查现有战斗动作。新姿态图尚未生成成功；正式战斗仍使用 flame-wing-v5，以下改动仅优化现有动作位移。

## 素材尝试

- 使用内置 imagegen，参考 `apps/web/public/sprites/pokemon/6.png`，要求 2×2 四姿态、统一体型与脚线、纯洋红底，后续按此前约定在本地去底。
- 完整提示词保存在工作区 `art-source/flame-wing-v7/front-prompt.txt`。
- 服务在输出审核阶段返回 HTTP 400，`moderation_blocked`，类别 `other`，请求 ID `deedcf55-46b8-4933-bd59-2a6ec8d1d943`；未提供更具体原因。本次没有返回新图片，也未更换正式资源或 manifest。

## 位移调整

喷火龙已配置横向攻击、释放和恢复轨道，渲染器此前又叠加通用前冲／后坐，使近战峰值由轨道的 21px 增至约 43px，施法释放由 14px 增至约 24px，产生过量身体滑动。

明确声明 `offsetX` 的动作轨道现在负责完整的横向前冲／后坐（包含显式的 0）。只配置纵向、缩放等轨道时仍使用通用横向动作，因此耿鬼等其他角色保持原表现。独立受击位移、技能编排位移和引擎位置继续各自生效。

这不等于新增头颈、张嘴、振翼关键帧；真正的姿态重绘仍待可用素材。

## 验证

- `npm run smoke`：通过。新增正反朝向的喷火龙近战 21px、施法 14px、后坐 -5px 验证，并检查耿鬼／皮卡丘通用动作、原动作时长和引擎快照不变；既有受击、连招、打断与挂点测试通过。
- `npm run typecheck`：通过。
- `npm run visuals:battle -- --skills-only`：通过。六类技能、两个朝向、释放与接触共 24 张截图，动作和效果正常结束，无浏览器错误。报告位于 `doc/visual-baselines/battle/skills-after/report.json`（生成时间 `2026-09-11T01:22:29.178Z`）。喷射火焰样例的释放 130ms、接触 210ms 沿用原时间轴。
- 产品独立验收：本轮“消除重复横向位移”通过。代码保留受击／技能编排，四张喷火截图中角色完整；没有将此改动视为新姿态素材完成。

## 尚存局限

反向喷火截图中的火焰起点仍在头颈上沿，高于张嘴位置（接触图约 20px）。本轮没有解决这一现有挂点与素材姿态的偏差；后续需与实际收颈、张嘴、恢复关键帧一起校准，不能据当前截图宣称嘴部准确贴合。
